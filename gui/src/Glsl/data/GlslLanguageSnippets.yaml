- label: "for(;;)"
  detail: "for(;;) {}"
  insertText: |-
    for (${1:init}; ${2:cond}; ${3:step}) {
      ${4:body}
    }
  documentation:
    value: "For Loop"
  kind: 25
  insertTextRules: 4

- label: "while()"
  detail: "while() {}"
  insertText: |-
      while (${1:cond}) {
        ${2:body}
      }
  documentation:
    value: "While Loop"
  kind: 25
  insertTextRules: 4

- label: "dowhile"
  detail: "do {} while()"
  insertText: |-
      do {
        ${2:body}
      } while (${1:cond});
  documentation: "While Loop"
  kind: 25
  insertTextRules: 4

- label: "ifelse"
  detail: "if () {} else {}"
  insertText: |-
      if (${1:condition}) {
        ${2:true}
      } else {
        ${3:false}
      }
  documentation:
    value: "If-Else Statement"
  kind: 25
  insertTextRules: 4

